


export const Token = (state = 
    {
        secretToken : null,
        isLoading : false,
        message  : null,
        status  : null
    }    
    , action ) => {
        switch(action.type) {
            default : return state;
        }
}